# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
release
*.local

# Local duplicate/generated app icons
/icon.ico
/icon.png

# SECURITY: Never commit license keys, private keys, or generated signing assets
.license/
*.pem
*.key
*.pfx
*.cer
*.p12
private-code-signing-backup*/
*code-signing*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (may contain secrets)
.env
.env.local
.env.*.local

# Internal dev handbook — NEVER `git add -f` (keep full doc outside public repo)
DEVELOPMENT_TEAM.md

# Internal task tracking and feature specs (keep local, not for public repo)
task-*.md
tasks-*.md

# Deployment license public key (never commit — distribute via private channel)
electron/license-public-key.cjs

# AI coding assistant workspace (Claude Code, Copilot, Codex, etc.)
.claude/
.codex-dev/
.codex/

# Scratch / temporary files
test-invoice.mjs

# Test coverage output (generated by vitest --coverage)
coverage/

# Test runner artifacts (Playwright + vitest reports, traces, screenshots, junit)
test-results/
/reports/
