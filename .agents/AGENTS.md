# Custom Rules for Antigravity Agent

- When the user says "pull" (or "اسحب"), immediately run `git pull` (or the resolved git binary path) to fetch and merge the latest updates from the GitHub repository into the workspace.
